#Quick function to determine if a number is a perfect square.

import math
def is_square(n):
    if n <= 0:
        return False
    elif match.sqrt(n) == math.sqrt(round(n)):
        return True
    else:
        return False
